#include "HAL_UART.h"

void move(MotorHandle *handle, MOTOR_DIR direction, uint8_t speed)
{
    handle = handle;
    direction = direction;
    speed = speed;
}

void stop(MotorHandle *handle)
{
    handle = handle;
}
